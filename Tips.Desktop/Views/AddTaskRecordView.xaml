<UserControl x:Class="Tips.Desktop.Views.AddTaskRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tips.Desktop.Views"
             xmlns:ctrls="clr-namespace:Tips.Desktop.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <ctrls:TextBoxWithTag Grid.Row="0" 
                              Tag="Day:" />
        
        <ctrls:TextBoxWithTag Grid.Row="1" 
                              Tag="Value:"
                              Text="{Binding Value, UpdateSourceTrigger=LostFocus}" />

        <ctrls:TextBoxWithTag Grid.Row="2" 
                              Tag="WorkValue:"
                              Text="{Binding WorkValue, UpdateSourceTrigger=LostFocus}"/>
    </Grid>
</UserControl>
