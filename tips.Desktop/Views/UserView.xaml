<UserControl x:Class="Tips.Desktop.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:ctrls="clr-namespace:Tips.Desktop.Views.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ctrls:UserIcon Grid.Row="0" MaxHeight="120"
               Source="{Binding User.IconFile}"/>

        <ctrls:LinkButton Grid.Row="1" VerticalAlignment="Center"
            Content="SelectIcon" Command="{Binding SelectIconFileCommand}" />

        <ctrls:LinkButton Grid.Row="2" Grid.Column="1"
            Content="Update" Command="{Binding UpdateCommand}" />
    </Grid>

</UserControl>
