<UserControl x:Class="tips.Desktop.Views.TopMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"       
             xmlns:ctrls="clr-namespace:tips.Desktop.Views.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style TargetType="ctrls:LinkButton">
            <Setter Property="Margin" Value="6" />
            <!--<Setter Property="Foreground" Value="Orange" />-->
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <ctrls:LinkButton Text="Home" Command="{Binding ToHomeCommand}" />
            <ctrls:LinkButton Text="{Binding Project.Name}" Command="{Binding ToProjectHomeCommand}" />
        </StackPanel>
        
    </Grid>
</UserControl>
