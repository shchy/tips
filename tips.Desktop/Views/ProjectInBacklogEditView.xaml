<UserControl x:Class="Tips.Desktop.Views.ProjectInBacklogEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"            
             xmlns:ctrls="clr-namespace:Tips.Desktop.Views.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Edit for Backlog" FontWeight="Bold" FontSize="16" />

        <TextBox Grid.Row="1" AcceptsReturn="True" Margin="6"
                 Text="{Binding EditText}"/>

        <ctrls:LinkButton Grid.Row="2" HorizontalAlignment="Right" Margin="6"
                          Content="Save"
                          Command="{Binding SaveCommand}" />

    </Grid>
</UserControl>
