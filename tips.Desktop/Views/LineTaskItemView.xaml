<UserControl x:Class="tips.Desktop.Views.LineTaskItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tips.Desktop.Views"
             xmlns:ctrls="clr-namespace:tips.Desktop.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ctrls:LabelWithTag Grid.Column="0"
                            Orientation="Horizontal"
                            TagContent="No."
                            TextContent="{Binding Id}" />

        <ctrls:LabelWithTag Grid.Column="1"
                            Orientation="Horizontal"
                            TagContent="Pt."
                            TextContent="{Binding Value}" />

        <ctrls:LabelWithTag Grid.Column="2"
                            Orientation="Horizontal"
                            TagContent=""
                            TextContent="{Binding Name}" />

    </Grid>
</UserControl>
